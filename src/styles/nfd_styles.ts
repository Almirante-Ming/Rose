import { StyleSheet } from "react-native";

export const nfd_styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#fff",
  },
  message: {
    fontSize: 20,
    textAlign: "center",
    marginTop: 50,
  },
});